fragment FeedDetails on Feed {
  id
  title
}

query GetFeeds {
  feeds {
    ...FeedDetails
  }
}

query EntriesByFeed($id: ID!) {
  entries(feed_id: $id) {
    title
    content
    id
  }
}

mutation CreateFeed($url: String!) {
  addFeed(url: $url) {
    ...FeedDetails
  }
}
