schema: ./lib/type-defs.ts
generates:
  ./lib/types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-operations
      - typescript-resolvers
config:
  typesPrefix: I
  avoidOptionals: true
